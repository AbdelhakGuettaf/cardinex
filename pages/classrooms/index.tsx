import React from "react";
import { ClassroomParent } from "../../components/Classroom/Classroom.parent";

interface indexProps {}

const index: React.FC<indexProps> = ({}) => {
  return <ClassroomParent />;
};

export default index;
